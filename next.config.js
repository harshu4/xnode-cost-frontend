/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  assetPrefix: 'https://openmesh-expert-frontend-prod.vercel.app',
}

module.exports = nextConfig
