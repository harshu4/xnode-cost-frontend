/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  assetPrefix: 'https://openmesh-expert-frontend-pord.vercel.app',
}

module.exports = nextConfig
